package org.poo.core.accounts;

import org.poo.core.User;

public class BusinessAccount extends Account {
    public BusinessAccount(String currency, String type, User user) {
        super(currency, type, user);
    }
}
